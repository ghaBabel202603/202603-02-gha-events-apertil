name: 02 - Eventos
on:
 - push:
 - pull_request:
 - schedule:
    - cron: "26 17 * * *"
    workflow_dispatch:
jobs:
  echo:
    runs-on: ubuntu-latest
    steps:
      - name: Show-Trigger
        run:
           echo Event name test: ${{ github.event_name }}